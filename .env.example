# === TELEGRAM ===
TELEGRAM_BOT_TOKEN=your-token-here
TELEGRAM_ALLOWED_USERS=your-user-id

# === AGENT PROVIDER ===
# Chọn 1: claude | openai | gemini | ollama | deepseek
# Không set → default "claude"
AGENT_PROVIDER=claude

# Model mặc định (để trống = provider default)
# Claude: claude-opus-4-6, OpenAI: gpt-4o, Gemini: gemini-2.5-pro
# Ollama: llama3.1, DeepSeek: deepseek-chat
AGENT_MODEL=

# API key cho provider (không cần cho claude subscription / ollama)
AGENT_API_KEY=

# Custom endpoint (cần cho ollama: http://localhost:11434)
AGENT_BASE_URL=

# === CLAUDE-SPECIFIC ===
# Chỉ dùng khi AGENT_PROVIDER=claude (hoặc không set)
CLAUDE_MODEL=claude-opus-4-6
# Hỗ trợ ~ → cross-platform, không cần sửa khi đổi Mac/Linux
CLAUDE_WORKING_DIR=~/projects

# === SESSION ===
SESSION_TIMEOUT_HOURS=72

# === GMAIL (optional, chỉ hoạt động với Claude provider) ===
# Lấy từ Google Cloud Console → OAuth 2.0 Client IDs
# Chạy: bun run scripts/gmail-auth.ts để lấy refresh token
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
